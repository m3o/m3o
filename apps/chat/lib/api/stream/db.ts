import { tableNames } from '../../constants'
import { m3o } from '../../m3o'

/**
 * type: message
 * userId: string
 * groupId: string
 * contextId: string
 * content: string
 */

type CreateInput = {
    userId: string
    contextId: string
    groupId: string
}

async function create() {
    // const response =
}
